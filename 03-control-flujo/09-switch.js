let acccion = 'listar' //ene l dado caso que lsitar no este

switch (acccion) {
    case  'listar':
        console.log('Accion de listar')
        //break
        case 'guardar':
            console.log('Accion de guardad')
            break
            default:
                console.log('Accion no reconocida')
}